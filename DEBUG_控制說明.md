# DEBUG 模式控制說明

## 如何開關 DEBUG 訊息

### 關閉 DEBUG（適合正式使用/前端整合）
在 `.env` 檔案中設定：
```
DEBUG_MODE=false
```

**效果：**
- ✅ 不會顯示 `[DEBUG] ...` 訊息
- ✅ 輸出乾淨，適合前端整合
- ✅ 使用者只看到 Agent 的回應

### 開啟 DEBUG（適合開發/除錯）
在 `.env` 檔案中設定：
```
DEBUG_MODE=true
```

**效果：**
- ✅ 顯示 LLM 提取資訊的過程
- ✅ 顯示狀態轉換
- ✅ 顯示過濾邏輯的中間結果
- ✅ 方便開發時除錯

## 目前狀態
✅ 已設定為 `DEBUG_MODE=false`（適合前端整合）

## 快速切換
只需要修改 `.env` 檔案的 `DEBUG_MODE` 值，不需要重啟程式。
